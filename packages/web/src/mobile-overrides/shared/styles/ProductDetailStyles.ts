/**
 * ProductDetailStyles Web Override
 * 
 * This override ensures that when ProductDetailScreen (from mobile package) 
 * runs on web, it uses the web-specific styles that respect mobile frame constraints
 */

export { createWebProductDetailStyles as createProductDetailStyles } from '../../../styles/WebProductDetailStyles';
